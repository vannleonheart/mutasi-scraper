

/**
 * Logger for bank process
 * @param {any} message
 * @returns {any}
 * @memberof Utils
 * @author fdciabdul
 * @utils Utils/Logger
 */
export default function log(message: any) {
    console.log(`[ LOG ] ${message}`);
}